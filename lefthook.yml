pre-commit:
  parallel: true
  commands:
    prettier:
      glob: 'src/**/*.{js,html,css}'
      run: npx prettier --write {staged_files} && git add {staged_files}
    eslint:
      glob: 'src/**/*.js'
      run: npx eslint --fix {staged_files} && git add {staged_files}
